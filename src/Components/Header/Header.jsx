// eslint-disable-next-line no-unused-vars
import React from 'react';
import "./Header.css"

const Header = () => {
    return (
        <div>
            this is Header
        </div>
    );
};

export default Header;